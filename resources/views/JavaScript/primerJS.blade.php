<!DOCTYPE html>
<html>

<head>
	<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
	<title></title>
</head>

<body>
	<script >
		/*dijimos que existe dos tipos de selectors, getElementById y getElementByTagName ()
		var imagenes=document.querySelectorAll(#carrete img)-> selecciona todos los selectores img que estan dentro del id carrete. array.length nos da el tamaño del array.
		arreglo[].style.visibility=hidden este esconde la imagen o producto dentro del arreglo en su indice
		jquery tiene dos caracteristicas tiene loop automatcicos, funciones anidadas
		*/
		$(document).ready(function(){
			$("#errores").html("<p>hay errores en el formulario</p>");
		});
</script>
</body>
</html>
<script >
	
	/*
		alert("HOla"); ventana de alerta
		document.write("hola alumnos");
		isNan();
		var nombre=prompt("ingresa  tu nombre");
		var articulos=[1,2,3];
 		y sus metodos son push=agregar al final,pop=elimfinal,unshift=agrgacomienzo,shif=elimcomienzo
		var articulos=[]; array vacio.
		function nombre(){codigo} se llama a la funcion creando una zona javascript y poniendo el nomb
		$(document).ready(function(){
			$("#errores").html("<p>hay errores en el formulario</p>");
		});reemplazA LO que esta dentro de #errores con lo de html.
		.append() agrega lo de append debajo de lo que hay dentro de #errores
		.prepend() agrega antes #errores, pero en el contenedor
		.before()   antes del contenedor de #errores
		.after()   despues del contenedor que contiene a #errores.
		.text()  lo mismo que html, pero no soporta tag html, no las reconoce
		$("#banner").revove()->borra el contenedor de id=banner
		$("#banner").replaceWith("<div>agregado al carro</div>")
		var z=document.getElementById("cerrar");
		z.addEventListener("click",cerrar_banner,false);
		haciendo de forma concatenada tendriamos
		.removeClass()
		.addClass()
		togleClass()
		IF(NUM.TARGET==IMG1) si se dio click en el objeto de id img1
	*/

</script>
